# Example - Clarification Gate

This example demonstrates structure only. Placeholder labels do not assert business
facts and must be replaced exclusively with approved corporate evidence.

## Known request

- Decompose `<monolith>` by the user-requested axis `<segment>`.
- Preserve `<approved business outcome>`.

## Questions required before decomposition

1. Which approved corporate source defines each segment?
2. Can one transaction cross segments, and where is that behavior evidenced?
3. Which system is authoritative for `<business concept>`?
4. Which outcomes must remain identical during migration?
5. Who approves the context boundaries and contract changes?

## Gate result

Status: **blocked pending answers**.

No target services are proposed until the questions are resolved and registered.
